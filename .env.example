# OneID 环境配置示例
# 使用说明：复制此文件为 .env 并根据实际情况修改
# 命令：cp .env.example .env

# ========================================
# 【必填】部署 URL 配置 - 最重要的配置！
# ========================================
# 设置为你的实际部署地址，必须与浏览器访问地址一致
# 
# 示例：
#   本地测试:     ONEID_URL=http://localhost:8080
#   局域网 IP:    ONEID_URL=http://192.168.1.100:8080
#   内网域名:     ONEID_URL=http://sso.company.local
#   公网 HTTPS:   ONEID_URL=https://sso.company.com
#
ONEID_URL=http://localhost:8080

# ========================================
# 数据库配置
# ========================================
# PostgreSQL 数据库密码
# 生产环境请使用强密码（建议 16+ 位字符，包含大小写字母、数字、特殊字符）
POSTGRES_PASSWORD=oneid_prod_password_change_me

# ========================================
# 应用端口
# ========================================
# OneID 对外暴露的端口（默认 8080）
# 如果修改端口，需要同时修改 ONEID_URL 中的端口号
ONEID_PORT=8080

# ========================================
# 管理员账号配置
# ========================================
# 首次启动时自动创建的管理员账号
# 生产环境请务必修改密码！
ADMIN_EMAIL=admin@oneid.local
ADMIN_USERNAME=admin
ADMIN_PASSWORD=ChangeMe123!

# ========================================
# 高级配置（通常不需要修改）
# ========================================
# OIDC 客户端 ID（默认：spa.portal）
# OIDC_CLIENT_ID=spa.portal

# OIDC 授权范围（默认：openid profile email offline_access）
# OIDC_SCOPES=openid profile email offline_access
